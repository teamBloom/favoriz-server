<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Event">
  <insert id="insertEvent">
    INSERT INTO event(name, date, type) VALUES(#{name}, #{date}, #{type})
  </insert>
</mapper>